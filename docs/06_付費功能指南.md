# Wevro 付費功能指南（RevenueCat）

> 這份文檔用於未來新增付費功能時參考。
> 第 1 版上架建議先用免費模式，累積用戶後再加入。

---

## 💰 付費方案設計

### 訂閱方案
- **Pro 訂閱：** $6.99/月
- **包含：** 每月 180 點 + 優先處理

### 點數包
| 名稱 | 價格 | 點數 |
|------|------|------|
| Small | $3.99 | 40 點 |
| Medium | $9.99 | 120 點 |
| Large | $24.99 | 300 點 |

---

## 🛠️ 使用 RevenueCat（推薦）

### 為什麼用 RevenueCat？
- ✅ 統一 iOS + Android 付費 API
- ✅ 自動處理收據驗證
- ✅ 免費額度：每月 $2,500 收入內免費
- ✅ 提供分析 Dashboard

### 安裝

```bash
npm install @revenuecat/purchases-capacitor
npx cap sync
```

### 環境變數

在 `.env` 新增：
```env
REVENUECAT_API_KEY=your_api_key_here
```

---

## 📦 產品設定

### 在 RevenueCat

1. 註冊：https://www.revenuecat.com/
2. 創建 App
3. 設定 Entitlements：
   - `pro` - Pro 訂閱權限
4. 設定 Products：
   - `wevro_pro_monthly`
   - `wevro_tokens_small`
   - `wevro_tokens_medium`
   - `wevro_tokens_large`

### 在 App Store Connect（iOS）

1. 創建訂閱產品
2. Product ID：`wevro_pro_monthly`
3. 價格：$6.99
4. 訂閱群組：Wevro Subscriptions

### 在 Google Play Console（Android）

1. 創建訂閱
2. Product ID：`wevro_pro_monthly`
3. 基本方案：$6.99/月

---

## 💻 前端整合範例

```typescript
// lib/purchases.ts
import Purchases from '@revenuecat/purchases-capacitor';

export async function initPurchases() {
  await Purchases.configure({
    apiKey: process.env.REVENUECAT_API_KEY,
  });
}

// 訂閱 Pro
export async function subscribeToPro() {
  const offerings = await Purchases.getOfferings();
  const proPackage = offerings.current?.monthly;
  
  if (proPackage) {
    const { customerInfo } = await Purchases.purchasePackage({
      aPackage: proPackage
    });
    
    // 檢查訂閱狀態
    return customerInfo.entitlements.active['pro'] !== undefined;
  }
  
  return false;
}
```

---

## ⏰ 實作時間估算

1. RevenueCat 設置：1-2 天
2. App Store/Play Console 產品設置：2-3 天
3. 前端整合：3-5 天
4. 後端整合：2-3 天
5. 測試：3-5 天

**總計：約 2-3 週**

---

## 💡 建議

### 第 1 版（現在）
- ❌ 不加付費功能
- ✅ 用免費模式累積用戶
- ✅ 收集反饋

### 第 2 版（1-2 個月後）
- ✅ 加入 RevenueCat
- ✅ 開始賺錢

**好處：** 降低風險，先驗證市場需求

---

**需要付費功能時再回來看這份文檔！** 💰

